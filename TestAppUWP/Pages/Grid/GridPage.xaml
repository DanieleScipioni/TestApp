<Page
    x:Class="TestAppUWP.Pages.Grid.GridPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppUWP.Pages.Grid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Pippo" HorizontalAlignment="Stretch" TextAlignment="Center"/>
        <ContentPresenter Grid.Row="1" 
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          
                          Background="Aquamarine"/>
        <ContentPresenter Grid.Row="0" Grid.RowSpan="2" Background="Green"
                          HorizontalAlignment="Stretch" VerticalContentAlignment="Center" VerticalAlignment="Stretch">
            <ProgressBar IsIndeterminate="True" Maximum="100" Value="50"
                             HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
        </ContentPresenter>
    </Grid>
</Page>
